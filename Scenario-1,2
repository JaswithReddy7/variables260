<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <script>
   /* let age=parseInt(prompt("Enter your age:"))
    if(age>=18){
        let ques=prompt("are you an indian citizen(yes/no)");
        if(ques.toLowerCase()=="yes"){
            alert("you are eligible to vote");
        }
        else if(ques.toLowerCase()=="no"){
            let dest=prompt("have you passed the citizenship test(yes/no)");
            if(dest.toLowerCase()=="yes"){
                alert("you are eligible to vote");
            }
            else{
                alert("you are not eligible to vote");
            }
        }
        else{
            alert("invalid input");
        }
    }
    else{
        console.log("You are not eligible to vote")
    }*/
  
   /*let a=prompt("Enter person A age:")

    
    if(a>=18){
        document.write("Can Vote","<br>")
    }
    else{
      document.write("Cannot Vote","<br>")
    }
    let b=prompt("Enter Yes if Citizen(1) or No(0):")
    if(b==0){
      
      let c=prompt("If passed Citizen test 'Enter Yes(1) or No(0)':")
    if(c==1){
      document.write("Can Vote ","<br>")
    }
    else if(c==0){
      document.write("Cannot Vote","<br>")
    }
    else if(b==1 && !(c==0)){
      document.write("Can Vote","<br>")
    }
    document.write("Cannot Vote","<br>")
    }
    else if(b==1){
      document.write("Can vote","<br>")
    }*/
    
    let a=prompt("Enter your Bill amount: ")
    let prem=prompt("are you a premium member:")
    if(a>=100){
      b=a*0.20
      document.write("Your Final bill after discount :"+(b=a-b))
    }
    else if(a>50 && a<100){
      c=a*0.10
      document.write("Your final bill is :"+(c=a-c))
    }
    else if(prem=="true" && a>100){
      d=a*0.30
      document.write("your bill is")
    }
    else if(prem="false"){
      c=a*0.10
      document.write("Your final bill is :"+(c=a-c))
    }

    else{
      document.write("No discount")
    }
  </script>
</body>
</html>
